import LoginForm from "../components/LoginPage/loginForm/loginForm";

const LoginPage = () => {
  return (
    <section>
      <LoginForm />
      <div>image here.</div>
    </section>
  );
};

export default LoginPage;
